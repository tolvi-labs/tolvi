---
tags: [session, sample]
date: 2026-05-02
status: active
---

## [11:00] Session — Token rotation work and the supersession

Finished the PASETO migration work that had been in flight since the prior week's benchmarks. Cut over the auth service to dual-issue JWT and PASETO tokens, with the resource servers already updated to accept either. Verification CPU on the middleware load test dropped by roughly 60%, which matched the local benchmark closely enough to feel confident. Wrote [[paseto-tokens]] as the replacement for [[jwt-vs-paseto-tokens]] and updated the older decision's frontmatter to point forward. The supersession is bidirectional — `supersedes` on the new doc, `superseded_by` on the old — so any tooling that walks the link graph stays consistent. Next session will track the cutover progress and remove JWT support once all resource servers report only PASETO traffic.
