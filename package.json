{
  "name": "tolvi",
  "private": true,
  "description": "Tooling and CI scripts for the Tolvi monorepo. Application code lives in cli/ and server/ with their own package.json files (Phase 2/3).",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate:schemas": "node .github/scripts/validate-schema.js",
    "validate:brand": ".github/scripts/brand-isolation-check.sh",
    "validate": "npm run validate:schemas && npm run validate:brand && npm run validate:installers && npm run validate:preflight && npm run validate:vault-health && npm run validate:prose",
    "validate:installers": ".github/scripts/installer-remediation-check.sh",
    "validate:preflight": ".github/scripts/preflight-sync-check.sh",
    "validate:vault-health": ".github/scripts/vault-health-parity-check.sh",
    "validate:prose": ".github/scripts/readme-em-dash-check.sh"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
